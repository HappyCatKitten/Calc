[package]
name = "brainfuck-calculator-core"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "A native Qt/QML calculator with a real Brainfuck numeric engine"
repository = "https://github.com/HappyCatKitten/brainfuck-calculator"
[lib]
crate-type = ["staticlib", "rlib"]
[dependencies]
num-bigint = "0.4"
num-traits = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
